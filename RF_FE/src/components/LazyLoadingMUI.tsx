import { Box } from "@mui/material";
import { LinearProgress } from "@mui/material";

export default function LazyLoadingMUI() {
  return (
    <Box sx={{ width: "100%" }}>
      <LinearProgress />
    </Box>
  );
}
